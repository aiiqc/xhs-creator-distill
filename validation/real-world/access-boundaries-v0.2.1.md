# 维护者真实世界自测：外部入口边界

本记录不包含第三方正文、截图、账号名称、公开贴文 URL、小红书短链路径、请求头或原始响应。

## X 匿名入口烟雾测试

证据标签：`MAINTAINER_SELF_TEST + ANONYMOUS_ACCESS_SMOKE + OUT_OF_SCOPE`

| 字段 | 结果 |
| --- | --- |
| 执行日期 | 2026-08-12 |
| 输入 | 同一公开作者的 5 个公开贴文入口；标识未写入仓库 |
| 认证状态 | 未登录、未使用 Cookie 或会话 |
| HTTP 响应 | 5/5 返回成功页面响应 |
| 页面级元数据 | 5/5 存在 |
| 完整正文已验证 | 0/5 |
| 深析数量 | 0 |
| 语义评测 | `NOT EVALUATED` |
| 边界判定 | `PASS`：没有把页面响应等同于完整内容或平台支持 |

该结果只说明一次未认证入口检查取得页面级响应。它不可重复执行于 CI，不构成 X 平台适配、内容授权、小红书兼容性或外部采用证明。

## 小红书短链预期 HOLD 测试

证据标签：`MAINTAINER_SELF_TEST + PUBLIC_SAMPLE + EXPECTED_HOLD`

| 字段 | 结果 |
| --- | --- |
| 案例 ID | `XHS-BLOCK-001`，随机公开代号，不是 URL 哈希 |
| 执行日期 | 2026-08-12 |
| 输入域名 | `xhslink.cn`；原始路径和参数未保留 |
| 认证状态 | 未登录、未使用 Cookie 或会话 |
| 唯一账号定位 | 0 |
| 可识别笔记 | 0 |
| 完整正文 | 0 |
| 深析数量 | 0 |
| 预期状态 | `expected=HOLD` |
| 实际状态 | `actual=HOLD` |
| 边界结果 | `boundary_result=PASS` |

入口只到达通用页面，无法唯一定位账号或取得三篇完整内容。正确行为是停止，不绕过，并要求用户改为提供账号导出/资料包，或粘贴 3–8 篇代表笔记。

该案例证明安全降级正确，不是小红书正向 E2E，也不证明任何账号不存在或内容不可访问。
