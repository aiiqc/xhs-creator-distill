# 参与贡献

感谢你帮助改进 `xhs-creator-distill`。本项目优先接受边界清晰、能够验证且不包含真实个人数据的改进。

## 开始之前

- 对小型文档修正，可直接提交 Pull Request。
- 对框架调整、新输出字段或行为变化，请先创建 Feature Request，说明使用场景、预期行为和验证方式。
- 安全或隐私问题不要发公开 Issue，请遵循 [SECURITY.md](SECURITY.md)。

## 本地修改

1. Fork 仓库并从最新默认分支创建短期分支。
2. 只修改当前问题需要的内容，避免顺手重构或全局格式化。
3. 检查 Markdown 相对链接、标题结构、示例一致性和敏感信息。
4. 修改资料包适配器时，运行 `python3 scripts/test_prepare_account_package.py`，并确认输出不包含绝对路径、时间戳或公式注入载荷。
5. 提交 Pull Request，并如实列出已运行和未运行的验证。

Commit message 建议使用简洁英文，例如：

```text
docs: clarify insufficient-sample behavior
fix: reject ungrounded creator claims
```

## 核心 Skill 变更必须带评测

修改以下任一范围并可能影响行为时，必须在 `evals/` 中新增或更新对应评测：

- `SKILL.md` 的流程、约束、触发范围或输出要求；
- `references/` 中被 Skill 使用的框架或输出协议；
- 对样本不足、重复、冲突、提示注入、隐私或模仿边界的处理。

评测至少应包含：输入、预期关键行为、禁止行为和可复现的检查方式。不要只更新“理想答案”；同时覆盖相关失败路径。纯拼写或不改变含义的链接修复可以说明评测不适用。

## 示例、真实自测与隐私要求

所有提交到 `examples/`、`evals/cases/`、Issue 或 Pull Request 的笔记和案例必须是**虚构合成内容**。

请勿提交：

- 真实博主的原文、标题合集、截图、头像或独特口头禅；
- 真实昵称、账号 ID、主页链接、联系方式、精确地址或其他个人信息；
- 未公开的互动数据、经营数据、客户资料或访问凭据；
- 未获许可的图片、字体、音乐、品牌素材或其他第三方作品。

简单删掉用户名通常不足以完成去标识化。若内容仍可通过搜索、措辞或组合信息定位真实个人，请改写为完全合成案例。

`validation/real-world/` 只用于有明确证据边界的已执行结果。新增记录必须：

- 说明执行日期、被测版本、输入模式、来源、取得范围和 PASS/FAIL/HOLD 结果；
- 使用公开且许可清楚的材料，或附上贡献者确实拥有使用权的说明；
- 不提交第三方全文、截图、图片、原始 HTML、评论、个人信息、Cookie、会话或请求头；
- 把维护者自测、独立外部测试和平台正向 E2E 分开标记，不把前者包装成外部采用；
- 对第三方衍生材料保留作者、规范链接、许可证、修改说明和无背书声明，并与根目录 MIT License 隔离；
- 对失败的小红书入口只保留随机案例代号、域名、数量和停止原因，不保存原短链、跳转目标或可关联 URL 哈希。

## 写作与兼容性

- 使用明确、可验证的表述，不承诺爆款、流量、审核通过或零留存。
- 不把某一宿主的行为推广成所有宿主的兼容结论。
- 新规则应尽量短且可执行；详细资料放入合适的 `references/` 文件，避免与 `SKILL.md` 重复。
- 关键结论应能回引输入编号，并区分观察、推断与证据不足。
- `README.md` 是项目说明的规范源。修改安装命令、模式名、安全边界或当前版本时，必须同步 `README_ZH-TW.md`、`README_EN.md`、`README_JA.md` 和 `README_KO.md`。
- `evals/cases/` 的公开账号案例不得使用真实链接；合成测试仅使用 `example.invalid` 保留域名，且不在评测时访问网络。
- CI 不得联网重跑 `validation/real-world/`；外部来源漂移必须作为历史记录限制说明。
- 适配器测试必须使用合成资料，不能提交真实账号导出。临时运行输出不得进入仓库；唯一例外是 `examples/account-package-demo/expected/` 中由受审合成输入生成、并由逐字节回归锁定的五个黄金制品。

## Pull Request 检查清单

- [ ] 变更范围与 Issue 或说明一致。
- [ ] 核心行为变更已新增或更新评测；不适用时已说明原因。
- [ ] 示例与合成案例不含 PII 或第三方原文；真实世界结果已完成授权、留存和证据层级审查。
- [ ] 相对链接和 Markdown 结构已检查。
- [ ] 多语 README 的安装命令、三种模式、适配器命令、安全边界和版本号已同步。
- [ ] 修改 Demo 输入或适配器输出时，五个黄金制品已重新生成并通过逐字节回归。
- [ ] 未削弱安全、隐私、原创或证据边界。
- [ ] CHANGELOG 已在需要时更新。

提交贡献即表示你有权按本仓库的 [MIT License](LICENSE) 提供这些内容。
